eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      name: product
      label: master
      discovery:
        enabled: true
        serviceId: configserver
