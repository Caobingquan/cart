eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      name: cartt
      label: master
      discovery:
        enabled: true
        serviceId: configserver